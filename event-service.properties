## Server Properties
server.port= 5001

## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url= jdbc:mysql://localhost:3306/event_service?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
spring.datasource.username=root
spring.datasource.password=password

## Hibernate Properties

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql=true

## Hibernate Logging

management.endpoints.web.exposure.include=*

#
spring.config.discovery.enabled: true
spring.config.discovery.service-id: config-server

# Disable gateway & redis as we don't need them in this application
spring.cloud.gateway.enabled=false
spring.cloud.gateway.redis.enabled=false

# Resource server settings
spring.security.oauth2.resourceserver.opaquetoken.introspection-uri=http://localhost:8083/auth/realms/lsa/protocol/openid-connect/token/introspect
spring.security.oauth2.resourceserver.opaquetoken.client-id=event-service
spring.security.oauth2.resourceserver.opaquetoken.client-secret={cipher}34c2dd1e2b306ec6ae544969cb08ec8a0e000978d862e31cf8a471d62fa95f0c243e88d812ee3ccad290cb6515ef3083e6aceee56fe39eccbad52f4fd5483f42

spring.security.oauth2.resourceserver.jwt.issuer-uri: http://localhost:8083/auth/realms/lsa/protocol/openid-connect/token